# 谛听 Lullaby · 分发仓库

安卓 AI 朗诵电子书阅读器「谛听」的资源分发仓库。**本仓库不含源码**，只托管 APK 与运行时按需下载的资源。

## 下载

最新版本 **[`v0.146.0`](../../releases/latest)** — `Lullaby-v0.146.0.apk`，45,216,455 bytes，仅 arm64-v8a，Android 8.0+。

已装旧版的用户直接覆盖安装即可（签名与既往版本一致，书库与阅读进度不受影响），或在应用内「设置 → 关于 → 检查更新」自动获取增量包。

## Releases

| Tag | 内容 | 是否可变 |
|---|---|---|
| `v0.146.0` | 最新版本：完整包 + 从 `v0.145.0` 的增量包 `.hdiff`（4,362,621 bytes） | ❌ |
| `v0.145.0` | 上一版本；作为 `v0.146.0` 增量包的基线**须长期保留** | ❌ |
| `v0.138.0` – `v0.144.0` | 历史版本，各自的完整包与增量包 | ❌ |
| `app-v1` | 长期固定通道：应用内更新清单 `app-version.json`、在线 TTS 端点发现 `online-endpoint.json`（内容随版本刷新，tag 不变） | ✅ |
| `fonts-v1` | 可下载中文字体（开源 OFL 授权） | ✅ 追加为主 |
| `models-d235-7c3cd722` | Piper 离线音色「小雅」 | ❌ 内容寻址 |
| `models-d223-18b35f2d` | Piper 离线音色「超文」 | ❌ 内容寻址 |

APK 按版本发布为 `vX.Y.Z` tag，文件名 `Lullaby-vX.Y.Z.apk`；存在可用基线时同时提供 `.hdiff` 增量包，应用内更新会自动择优下载。**版本化二进制与内容寻址的音色包一经发布绝不覆盖**——客户端用硬编码 SHA-256 校验下载结果，原地替换会让已发布的各版本同时校验失败。

## 校验

每个版本的完整包与增量包的字节数、SHA-256 与签名证书指纹，记录在源码仓的 `RELEASE.md` 中。当前版本：

| 资产 | 大小 | SHA-256 |
|---|---|---|
| `Lullaby-v0.146.0.apk` | 45,216,455 | `b7b6ffc0ea87152f88a681af17ad4268d776cb96e5325e9b88afed9314064509` |
| `Lullaby-v0.145.0-to-v0.146.0.hdiff` | 4,362,621 | `3e27720bfcc605cad4095a1b5ea21ca32427008d4e40637c638ce58d4381bb26` |

签名证书 SHA-256 `176a676f7ede27a17aa94316add70230e12e7716536f66d62506f6b27f069265`，自 `v0.130.0` 起各版本一致。

## 说明

- 离线音色不内置于 APK，在应用内按需下载；音色包按内容寻址托管，内容变更即启用新 tag。
- 语音模型授权 CC-BY-NC，**非商用**。
- 源码、文档与问题追踪在 `Kaibancus/Lullaby`（私有仓库）。

> **2026-07-28 仓库改名**：本仓库与源码仓库的名字已对调（本仓库原名 `Lullaby`，源码仓原名 `Audiobook`）。`v0.137.0` 及更早版本的客户端内置的是旧地址，**已永久失效**，需手动安装一次 `v0.138.0` 或更新版本，此后即可恢复应用内自动更新。
